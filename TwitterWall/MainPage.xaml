<UserControl 
    x:Class="TwitterWall.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:TwitterLib;assembly=TwitterLib"
    xmlns:mvvm="clr-namespace:GalaSoft.MvvmLight.Command;assembly=GalaSoft.MvvmLight.WP7"
    mc:Ignorable="d">
    
    <UserControl.DataContext>
        <local:SearchViewModel />
    </UserControl.DataContext>

    <Grid x:Name="LayoutRoot" Background="White" Width="Auto">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBox Text="{Binding Path=Keyword, Mode=TwoWay}" />
            <Button 
                Content="Search"
                mvvm:ButtonBaseExtensions.Command="{Binding Path=SearchCommand}"/>
        </StackPanel>
        
        <ItemsControl Grid.Row="1" 
                      ItemsSource="{Binding Path=Tweets}" 
                      DisplayMemberPath="Text" />        
    </Grid>
</UserControl>
